# ============================================================
# المرحلة 0 — الأساس (ابدأ من هنا)
# Phase 0 — Foundation (Start Here)
# تثبيت: pip install -r requirements_phase0.txt
# ============================================================

# --- الاتصال بـ MT5 ---
MetaTrader5>=5.0.45

# --- معالجة البيانات ---
pandas>=2.1.0
numpy>=1.26.0
pyarrow>=14.0.0          # حفظ البيانات بصيغة Parquet

# --- قاعدة البيانات ---
sqlalchemy>=2.0.0        # ORM للتعامل مع SQLite
aiosqlite>=0.19.0        # SQLite غير متزامن

# --- المؤشرات التقنية ---
pandas-ta>=0.3.14b       # RSI, MACD, ATR, Bollinger Bands...
ta>=0.11.0               # مكتبة مؤشرات بديلة

# --- التسجيل والمراقبة ---
loguru>=0.7.2            # سجلات منظمة وجميلة
rich>=13.7.0             # طباعة ملونة في الطرفية

# --- الإعدادات والمتغيرات ---
python-dotenv>=1.0.0     # قراءة ملف .env
pyyaml>=6.0.1            # قراءة ملفات YAML
pydantic>=2.5.0          # التحقق من صحة البيانات
pydantic-settings>=2.1.0

# --- الجدولة الزمنية ---
apscheduler>=3.10.4      # جدولة المهام الدورية

# --- طلبات HTTP ---
requests>=2.31.0
httpx>=0.25.0            # HTTP غير متزامن
