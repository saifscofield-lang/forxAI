# ============================================================
# المرحلة 2 — الذكاء الاصطناعي والتعلم الآلي
# Phase 2 — AI & Machine Learning
# تثبيت: pip install -r requirements_ml.txt
# (ثبّت requirements_phase0.txt أولاً)
# ============================================================

# --- تعلم آلي أساسي ---
scikit-learn>=1.3.2      # خوارزميات ML الأساسية، التحقق المتقاطع

# --- تعزيز التدرج (الأقوى للبيانات الجدولية) ---
lightgbm>=4.2.0          # الأسرع والأكثر دقة — الخيار الأول
xgboost>=2.0.2           # بديل قوي
catboost>=1.2.2          # ممتاز للمتغيرات الفئوية

# --- السلاسل الزمنية والإحصاء ---
statsmodels>=0.14.1      # ARIMA, VAR, اختبارات إحصائية
arch>=6.3.0              # نماذج GARCH لتقلب الأسعار
scipy>=1.11.4            # حسابات علمية متقدمة

# --- كشف النظام السوقي (Regime Detection) ---
hmmlearn>=0.3.0          # نموذج ماركوف المخفي HMM

# --- التعلم العميق ---
torch>=2.1.2             # PyTorch — نماذج LSTM, Transformer
# ملاحظة: للنسخة مع GPU استخدم:
# pip install torch --index-url https://download.pytorch.org/whl/cu118

# --- تتبع التجارب ---
mlflow>=2.9.2            # تسجيل النماذج والتجارب

# --- تحسين المعاملات الفائقة ---
optuna>=3.5.0            # البحث البايزي عن أفضل المعاملات

# --- تفسير النماذج ---
shap>=0.44.0             # شرح قرارات النموذج (SHAP values)

# --- كشف الانجراف ---
evidently>=0.4.11        # مراقبة جودة النموذج في الإنتاج
