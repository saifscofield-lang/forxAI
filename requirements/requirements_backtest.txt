# ============================================================
# الاختبار الخلفي والتحليل
# Backtesting & Analytics
# تثبيت: pip install -r requirements_backtest.txt
# ============================================================

# --- الاختبار الخلفي ---
backtrader>=1.9.78.123   # محرك اختبار خلفي مدفوع بالأحداث
vectorbt>=0.25.2         # اختبار خلفي متجه (سريع جداً)

# --- تحليل الأداء ---
pyfolio-reloaded>=0.9.5  # تقارير الأداء التفصيلية (Tearsheet)
quantstats>=0.0.62       # إحصاءات التداول الكمي

# --- تصوير البيانات ---
matplotlib>=3.8.2
seaborn>=0.13.0
plotly>=5.18.0           # رسوم بيانية تفاعلية
