# دليل استخدام ForexAI Dashboard

---

## التشغيل

| الإجراء | الطريقة |
|---------|---------|
| **تشغيل كل شيء** | نقرة مزدوجة على `start.bat` |
| **إيقاف كل شيء** | نقرة مزدوجة على `stop.bat` |
| **فتح الداشبورد يدوياً** | افتح المتصفح على `http://localhost:8501` |

> **قبل التشغيل:** تأكد أن MT5 Terminal مفتوح وأنت مسجّل دخول على حسابك.

---

## الشاشات السبع

---

### 🏠 الرئيسية (Home)

**ما تراه:**
- **6 مؤشرات رئيسية** في الأعلى: عدد الصفقات، نسبة الربح، إجمالي P&L، Profit Factor، الصفقات المفتوحة، أقصى خسارة
- **منحنى Equity** — يُظهر نمو الحساب عبر الزمن
- **Drawdown** — الانخفاض من أعلى قيمة للحساب
- **Signal Outcomes** — كيف توزّعت الإشارات (منفّذة / محجوبة ML / مرفوضة)
- **Win/Loss Pie** — نسبة الصفقات الرابحة للخاسرة
- **جدول الأداء بالزوج** — مقارنة EURUSD، GBPUSD، USDJPY، XAUUSD
- **آخر الإشارات** — أحدث 10 إشارات

**زر Refresh Data** في الشريط الجانبي — يجدّد كل البيانات.

---

### 📡 السكانر (Scanner)

**الغرض:** فحص السوق يدوياً وتنفيذ الصفقات.

**خطوات الاستخدام:**
1. اختر الأزواج التي تريد فحصها من الشريط الجانبي
2. فعّل أو أوقف **ML Filter** (موصى به: مفعّل)
3. اضغط **🔍 Scan Now**
4. انتظر 5-15 ثانية حتى يتصل بـ MT5 ويفحص الأزواج

**قراءة النتائج:**
- **Active Signals** — إشارات جاهزة للتنفيذ
  - اللون الأخضر = BUY | الأحمر = SELL
  - يظهر: سعر الدخول، Stop Loss، Take Profit، نسبة Risk:Reward
  - ML Confidence — مدى ثقة الذكاء الاصطناعي بالصفقة (مثال: 67%)
  - اضغط **▶ Execute** لتنفيذ الصفقة مباشرة على MT5
- **Filtered Signals** — إشارات رآها النظام لكن حجبها ML لأن الثقة أقل من الحد المطلوب
- **Raw Data** — البيانات الخام بصيغة جدول مع خيار تصدير CSV

**الرسم البياني:**
- اختر زوجاً وإطاراً زمنياً ثم اضغط **Load Chart** لرؤية الشموع اليابانية.

---

### 📊 الصفقات المفتوحة (Positions)

**الغرض:** متابعة الصفقات الحالية على حسابك.

**ما تراه:**
- **ملخص الحساب** — الرصيد، الإكويتي، P&L المفتوح، الهامش
- **جدول الصفقات** — كل صفقة مفتوحة مع: الزوج، الاتجاه، سعر الدخول، السعر الحالي، الربح/الخسارة
- الأرقام الخضراء = ربح | الحمراء = خسارة

**إغلاق صفقة:**
1. في قسم **Close Position**، اختر الصفقة من القائمة
2. اضغط **❌ Close Position**
3. سيُغلَق الأمر فوراً بسعر السوق الحالي

**حاسبة حجم الصفقة:**
- أدخل رصيد حسابك، نسبة المخاطرة، حجم الـ Stop Loss بالـ pips
- يحسب لك تلقائياً: حجم اللوت المناسب وقيمة كل pip

---

### 📋 سجل الصفقات (Trade History)

**الغرض:** مراجعة كل الصفقات المغلقة.

**الفلاتر المتاحة:**
- **Symbol** — فلتر بزوج محدد
- **Direction** — BUY أو SELL فقط
- **Strategy** — SMA Crossover أو ML Filtered
- **Result** — الرابحة فقط أو الخاسرة فقط

**ما تراه:**
- **KPIs** — إجمالي الصفقات، نسبة الربح، مجموع P&L، متوسط P&L
- **Cumulative P&L** — منحنى تراكم الأرباح لكل زوج
- **P&L Distribution** — توزيع الأرباح والخسائر (histogram)
- **الجدول الكامل** — كل صفقة مع تفاصيلها
- **Exit Reason Breakdown** — كم صفقة أُغلقت بـ SL، كم بـ TP، كم يدوياً

**تصدير البيانات:** زر **⬇ Export CSV** لتحميل السجل كملف Excel.

---

### 🔍 سجل الإشارات (Signal Log)

**الغرض:** مراجعة كل إشارة توليدها النظام — سواء نُفّذت أو حُجبت.

**الحالات الثلاث:**
| الحالة | المعنى |
|--------|--------|
| `EXECUTED` | نُفّذت الصفقة على MT5 |
| `ML_FILTERED` | الإشارة موجودة لكن ثقة ML أقل من الحد |
| `RISK_REJECTED` | رفضها مدير المخاطر (تجاوز الحد الأقصى للصفقات المفتوحة أو الخسارة اليومية) |

**الرسوم البيانية:**
- **Daily Signal Volume** — كم إشارة يومياً ومن أي نوع
- **ML Confidence Distribution** — توزيع نسب الثقة للإشارات الناجحة والمحجوبة

**جدول ML Filter Impact** — يُظهر لكل زوج: كم إشارة مرّت، كم حُجبت، متوسط الثقة.

---

### 📈 التحليل (Analytics)

**الغرض:** تحليل عميق للأداء الكلي.

**ما تراه:**

| القسم | المحتوى |
|-------|---------|
| **6 KPIs** | عدد الصفقات، Win Rate، Net P&L، Profit Factor، Sharpe Ratio، Max Drawdown |
| **Equity Curve** | منحنى نمو الحساب مع خيار إخفاء/إظهار خط الرصيد |
| **Drawdown** | الانخفاضات عن القمة بالنسبة المئوية |
| **Monthly P&L** | شريط أخضر/أحمر لكل شهر |
| **Win/Loss Pie** | نسبة الصفقات الرابحة دائرياً |
| **Cumulative by Symbol** | منحنى تراكم الربح لكل زوج منفصلاً |
| **P&L Distribution** | histogram يُظهر توزيع الأرباح والخسائر |
| **Symbol Radar** | مقارنة الأزواج بمخطط العنكبوت |
| **Advanced Stats** | أفضل/أسوأ صفقة، أطول سلسلة ربح/خسارة، Expectancy، Payoff Ratio |
| **Duration Scatter** | مدة الصفقة مقابل الربح — هل الصفقات الطويلة أكثر ربحاً؟ |

---

### 🤖 رؤى ML (ML Insights)

**الغرض:** فهم أداء نموذج الذكاء الاصطناعي.

**ما تراه:**

**جدول Walk-Forward Results:**
نتائج التحقق الحقيقي للنموذج (تدريب 2010-2022، اختبار 2023-2026):

| الزوج | بدون ML | مع ML | الحكم |
|-------|---------|-------|-------|
| EURUSD | PF 1.33 | PF 1.48 | ✅ يستخدم ML |
| GBPUSD | PF 1.13 | PF 1.23 | ✅ يستخدم ML |
| USDJPY | PF 1.28 | PF 0.99 | ❌ بدون ML |
| XAUUSD | PF 1.40 | PF 1.60 | ✅ يستخدم ML |

**Feature Importance:**
- اختر زوجاً لترى أهم المتغيرات التي يستخدمها النموذج في قراراته
- أهم المتغيرات عادة: `sma_50_200_diff`، `atr_14_pct`، `close_vs_sma_200`

**ML Confidence vs Profitability:**
- صندوقي box plot يُظهر هل الصفقات الرابحة لها confidence أعلى فعلاً

**Model Files Status:**
- جدول يُظهر حالة ملفات النماذج المحفوظة في `data/models/`

---

### ⚡ الباكتست (Backtest)

**الغرض:** اختبار أي استراتيجية على بيانات تاريخية قبل تطبيقها.

**خطوات الاستخدام:**
1. اختر الزوج والإطار الزمني
2. اضغط **📥 Load Optimized Params** لتحميل أفضل باراميترات معروفة
3. أو عدّل الباراميترات يدوياً:
   - **SMA Fast / Slow** — فترات المتوسطات المتحركة
   - **RSI Period** — فترة مؤشر RSI
   - **ATR Period** — فترة مؤشر ATR
   - **SL / TP Multiplier** — مضاعف الـ Stop Loss والـ Take Profit بالنسبة لـ ATR
4. اضغط **▶ Run Backtest**

**النتائج:**
- **6 KPIs**: عدد الصفقات، Win Rate، Profit Factor، Total P&L، Max Drawdown، Sharpe
- **منحنى Equity** للاختبار
- **Trade Log** — كل صفقة في الباكتست مع خيار تصدير CSV

**الباراميترات المحسّنة المرجعية:**
جدول في أسفل الصفحة يُظهر أفضل باراميترات وجدها Optuna لكل زوج.

---

## الشريط الجانبي (Sidebar)

موجود في كل الصفحات ويُظهر:
- **MT5 Connected / Disconnected** — حالة الاتصال
- **رقم الحساب، الرصيد، الإكويتي، P&L** — إذا كان متصلاً
- **وضع التداول** — PAPER (تجريبي) أو LIVE
- **زر 🔄 Refresh Data** — يمسح الكاش ويجدّد كل البيانات

---

## الأزرار الهامة

| الزر | أين يوجد | ما يفعله |
|------|----------|---------|
| 🔍 Scan Now | Scanner | يفحص السوق ويجلب الإشارات من MT5 |
| ▶ Execute | Scanner | ينفّذ الصفقة فوراً على MT5 |
| ❌ Close Position | Positions | يُغلق صفقة مفتوحة بسعر السوق |
| 📥 Load Optimized Params | Backtest | يملأ الباراميترات بأفضل قيم معروفة |
| ▶ Run Backtest | Backtest | يُشغّل الباكتست ويُظهر النتائج |
| ⬇ Export CSV | History / Log / Backtest | يُحمّل البيانات كملف Excel |
| 🔄 Refresh Data | Sidebar | يُجدّد كل البيانات في الداشبورد |

---

## ملاحظات مهمة

- **الداشبورد لا يُداول تلقائياً** — هو للعرض والتنفيذ اليدوي فقط
- **البوت يُداول تلقائياً** — في نافذة "ForexAI Bot" المنفصلة كل ساعة
- **إذا MT5 غير متصل** — ستظهر البيانات من قاعدة البيانات المحلية فقط
- **البيانات تُحدَّث كل 15 ثانية** تلقائياً للصفقات المفتوحة
- **كل الصفقات محفوظة** في `data/trading.db` حتى بعد إغلاق الداشبورد
